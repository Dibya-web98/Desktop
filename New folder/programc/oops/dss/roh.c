#include<stdio.h>
#include<stdlib.h>
struct node{
    int data;
    struct node* next;
};
int main(){
    struct node*head=null;
    head=(struct node*)malloc(size of(struct node));
    head->data=20;
    head->next=null;
    printf("%d",head->data);
    printf("%d",head->next);
    printf("%d",head);
 return 0;   
}